2026.4.0 - 2026-08-07

- 将 BLE 公开身份改为每次启动时从默认蓝牙控制器 MAC 地址后六位自动派生，例如 `yundrone-12abcd`，解决批量复制系统盘造成设备名重复的问题。
- 移除设备别名、随机码和名称文件持久化；安装器会清理旧 `/var/lib/yundrone/ble-device-name`，并显示蓝牙 MAC、SN 与最终设备名。
- Server 在 Linux 蓝牙控制器延迟枚举时等待最多 60 秒；超时记录诊断身份 `yundrone-null` 后退出，由 systemd 自动重试。
- Rust Client、GUI 与 Web Client 继续宽松识别历史名称，并明确支持新的 MAC 派生名称和 `yundrone-null` 诊断名称。
- 加固公网分发流程，要求 client/server 五个平台包完整存在，并在上传后验证四份线上 manifest、资产 URL 和 SHA256。
- 统一仓库、GitHub Release、launcher、server installer 及 client/server 分发版本为 `2026.4.0`。

2026.3.0 - 2026-06-04

- 发布统一 BLE Wi-Fi 工具入口与 Gum TUI 安装体验更新，刷新 launcher、server installer 与公网安装 manifest。
- 更新 WebBluetooth 配网工作台，加入中英文切换、默认英文、语言偏好持久化、蓝牙 favicon 与更稳的 Header 布局。
- 完善 Web 工作台交互反馈、通知、基本信息、Wi-Fi 配网/管理和调试抽屉体验，并部署到 `https://tool.yundrone.cn/ble/`。
- 同步客户端、服务端与安装器分发版本到 `2026.3.0`。

2026.2.3 - 2026-05-27

- 将正式主线传输协议回退到 360B JSON UART / `response_json` chunking / `link.ack`，撤出误合入的 20B compact binary transport 实验方案。
- 保留 `accepted / progress / result` 业务事件语义；长任务 progress 重新作为普通 JSON 响应事件发送，不再使用 header-only control frame。
- 新增并保留 Carbon Design System 风格的静态 WebBluetooth 配网工作台，支持浏览器连接、Wi-Fi 扫描/配网、profiles/raw/diagnostics 和 Safe Verbose trace。
- 更新 README、命令契约、MTU 分片、BLE 调试器、兼容 server 和 Web client 文档，明确 360B JSON chunking 是正式主路径。
- 将 20B compact transport 成果归档到 `experiment/ble-transport-v2-20b` 与 `v2026.2.3-20b-experiment`，供微信小程序 BLE 4.0 方向后续研究。
- 新安装和重置 BLE 名称时支持 4 位用户别名 + 4 位随机码，例如 `yundrone-lab1-k9x8`；升级时继续保留旧 6 位名称。
- Rust Client、GUI 与 Web Client 放宽设备名匹配，不再限制后缀位数，并继续识别方括号里的 YunDrone local name。
- Server 安装器新增 `--name-alias`，TUI 会提示用户记住最终 BLE 名称，方便网页、CLI 和小程序配网时选择正确设备。

2026.2.2 - 2026-05-27

- 修复 Linux 客户端预编译包在 Ubuntu 20.04 上因 GLIBC 版本过高无法启动的问题，改用 Debian bullseye 构建并验证最高依赖不超过 GLIBC_2.31。
- 优化一键工具的客户端缓存校验，同版本包 SHA256 变化时会自动刷新本地缓存，并在启动前执行本机兼容性自检。
- 重构交互式客户端扫描界面，避免全屏清屏导致终端 scrollback 快速膨胀；非 TTY 下改为低频扫描日志。
- 改进 Linux Docker 构建缓存隔离，避免不同基础镜像复用旧 build-script 产物导致构建失败。

2026.2.1 - 2026-05-20

- 增强 `yundrone-ble-client interactive` 调试体验，扫描阶段加入动态 loading、倒计时和候选设备即时刷新，支持按 Enter 提前进入选择。
- 新增 `interactive --verbose` 原包观测模式，在交互式菜单执行命令时展示 TX/RX、分片重组与 QoS ACK 细节。
- 新增 `interactive --verbose-unsafe-raw` 危险开关；默认 verbose 输出会脱敏 Wi-Fi 密码等敏感字段，避免调试日志泄露凭据。
- 将 verbose 原包输出整理为单包单框的 Rich-like 视图，保留完整 packet 内容，并提升长 JSON、分片和 ACK 日志的可读性。
- 修复 CLI 在 macOS 下误触发 `.app` relaunch 的问题，使 `yundrone-ble-client` 保持纯终端调试工具行为。
- 更新兼容 BLE Server 实现指南为 v26.2.0 快照文档，补齐 V2.1 QoS、分片 ACK 和服务端实现注意事项。

2026.2.0 - 2026-05-19

- 升级 BLE 通讯协议到 V2，统一命令命名为 `领域.动作` 风格，并引入 `accepted / progress / result` 事件式响应。
- 新增 Wi-Fi 记忆管理能力，支持列出 NetworkManager 已保存 profiles，并按 UUID 批量删除非 active profile。
- 重构 BLE 公开身份治理，使用持久化单一 local name `yundrone-<base36_6>`，同步应用到 advertising Local Name、Adapter Alias 和状态展示字段。
- 修复 Bluefruit / WebBluetooth UART 过滤场景，确保广播元数据携带 Nordic UART Service UUID，同时保持只有一个 BLE 名字。
- 统一 client/server Cargo package 与二进制命名为 `yundrone-ble-client`、`yundrone-ble-server`，并整理 Typer-like CLI help、examples 与调试命令。
- 完善 server systemd 日志、分片观测、配对关闭、设备名持久化、公开部署文档与兼容 BLE server 对接文档。
- 重写 README / README_ZH 为快速使用、server 部署、client 调试和文档导航入口，并清理开源前不应公开的本地脚本、生成物和私有示例。

2026.1.1 - 2026-05-18

- 切换 Linux server 到可发现性优先的 BLE identity 模型，并引入更明确的广播策略与部署验收口径。
- 新增可选择的 Linux advertising backend，为 BlueZ D-Bus interval 不可靠的设备提供 legacy HCI 路径。
- 更新 client 扫描发现逻辑，使 GUI/CLI 能更稳定识别 YunDrone BLE gateway 候选设备。
- 将 Linux systemd 服务统一命名为 `yundrone-ble-command-gateway.service`。
- 更新部署、Release 和调试文档，说明 macOS app 资产、Linux/Windows 当前预编译支持范围与 BLE 广播排障方式。

2026.1.0 - 2026-05-17

- 新增结构化多网卡 IPv4 状态、首选 Wi-Fi IP 展示，并同步到 CLI 与 GUI 诊断结果。
- 增加 GUI 心跳异常后的自动断开恢复、手动断开反馈和前台动作状态提示。
- 增加 GUI Light、Dark、System 主题偏好持久化。
- 扩展 CI 构建验证覆盖 Ubuntu x64、Ubuntu arm64、macOS arm64 与 Windows x64。
